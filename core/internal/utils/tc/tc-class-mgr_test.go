package tc

import (
	// "log"
	"testing"
	// "github.com/stretchr/testify/assert"
)

func Test_TcClassMgr_findClass(t *testing.T) {
	// mgr, err := NewTcClassMgr("br-lan", 100, 100)
	// assert.Nil(t, err)

	// _, err = mgr.CreateClass(3, 100, 100, 0, 0)
	// assert.Nil(t, err)

	// var childId TcClassId = 3
	// prioChild := mgr.FindClass(childId)
	// log.Println(prioChild)
	// assert.Equal(t, childId, prioChild.ClassId)
	// assert.Equal(t, prioChild.Parent.ClassId, mgr.rootClass.ClassId)
}
